public enum EnumElement { Air, Earth, Fire, Water, Light }

public enum PowerUpType { Potion, Air, Earth, Fire, Water, Light }

public enum BattleStatus { Running, Victory, Defeat }

public enum BattleBossType { Mystic, Fighter }

public enum AttributeType { Air, Earth, Fire, Water, Light, Health }

public enum CreateSpellType { Attack, Clear, FreezeTime, Health, Protection, Time }

public enum EquipmentButtonType { None, Buy, Equip }